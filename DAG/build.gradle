apply plugin: 'java' 
apply plugin: 'application'

repositories {
    mavenCentral()
}

dependencies {
    compile files('lib/korat.jar');
    compile 'junit:junit:4.12'
}

mainClassName = "korat.Korat"

run {
    classpath 'lib/*'

    jvmArgs = ["-noverify"]

    //    if (project.hasProperty("appArgs")) {
    args Eval.me("['--class', 'DAG.DAG', '--args', '3']")
    //    }
}
